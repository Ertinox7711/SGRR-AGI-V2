# snippets/ — __SHOP_NAME__

Mêmes règles que `sections/` : voir [`../sections/CLAUDE.md`](../sections/CLAUDE.md).

Rappel : le **live est l'autorité** (`node scripts/pull-theme-file.js snippets/x.liquid --diff`), push via `node scripts/push-any.js snippets/x.liquid --go`.
